const express = require('express');
const router = express.Router();

const express = require('express')'';
const { PrismaClient } = require('@prisma/client')'';
router.get('/', '';
    res.json({ "message": 'data-import endpoint работает;
    console.error('Ошибка получения data-"import":;
    res.status(500).json({ "error": Ошибка сервера;
router.get(/:id, ;
    res.json({ "message": 'data-import по ID;
    console.error('Ошибка получения data-"import":;
    res.status(500).json({ "error": Ошибка сервера;
router.post(/, authenticateToken, ;
    res.status(201).json({ "message": 'data-import создан;
    console.error('Ошибка создания data-"import":;
    res.status(500).json({ "error": 'Ошибка сервера;
}}}}}}))))))))))))

module.exports = router;