const express = require('express');
const router = express.Router();

const router = require('express').Router();
const express = require('express')'';
const { PrismaClient } = require('@prisma/client')'';
router.get('/', '';
    res.json({ "message": 'warehouse endpoint работает;
    console.error('Ошибка получения "warehouse":;
    res.status(500).json({ "error": Ошибка сервера;
router.get(/:id, ;
    res.json({ "message": 'warehouse по ID;
    console.error('Ошибка получения "warehouse":;
    res.status(500).json({ "error": Ошибка сервера;
router.post(/, authenticateToken, ;
    res.status(201).json({ "message": 'warehouse создан;
    console.error('Ошибка создания "warehouse":;
    res.status(500).json({ "error": 'Ошибка сервера;
}}}}}}))))))))))));
module.exports = router;


module.exports = router;