# 🚀 VHM24 Environment Variables Example
# Используйте этот файл как шаблон для создания .env файла

# === КРИТИЧЕСКИ ВАЖНЫЕ ПЕРЕМЕННЫЕ ===
# Порт для запуска сервера (обязательно)
PORT=3000

# Публичный URL приложения в Railway (обязательно для production)
RAILWAY_PUBLIC_URL=https://your-app-name.up.railway.app

# Окружение приложения: development, test или production
NODE_ENV=production

# === DATABASE ===
# URL подключения к PostgreSQL (обязательно)
# Формат: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/vendhub

# === REDIS ===
# URL подключения к Redis (обязательно)
# Формат: redis://username:password@host:port
REDIS_URL=redis://default:password@localhost:6379

# === TELEGRAM BOT ===
# Токен Telegram бота, полученный от @BotFather (обязательно)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# ID администраторов в Telegram, через запятую
ADMIN_IDS=123456789,987654321

# URL для webhook Telegram бота (для production)
WEBHOOK_URL=https://your-app-name.up.railway.app/api/telegram/webhook

# === SECURITY ===
# Секрет для JWT токенов (обязательно)
# Сгенерировать: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key

# === S3 STORAGE (DIGITALOCEAN SPACES) ===
# Endpoint S3 хранилища
S3_ENDPOINT=https://fra1.digitaloceanspaces.com

# Регион S3 хранилища
S3_REGION=fra1

# Название бакета для хранения файлов
S3_BUCKET=your-bucket-name

# Ключ доступа к S3 хранилищу
S3_ACCESS_KEY=your_s3_access_key

# Секретный ключ к S3 хранилищу
S3_SECRET_KEY=your_s3_secret_key
